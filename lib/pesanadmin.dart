// pesanadmin.dart
import 'package:flutter/material.dart';

class PesanAdmin extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Pesan Admin'),
        backgroundColor: Color(0xFF3158DA),
      ),
      body: Center(
        child: Text('Halaman Pesan Admin'),
      ),
    );
  }
}
